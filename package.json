{
  "name": "worldcup",
  "private": true,
  "version": "1.0.0",
  "description": "2026 북중미 월드컵 결선 토너먼트 대진표",
  "scripts": {
    "build": "esbuild src/main.jsx --bundle --minify --jsx=automatic --outfile=app.js",
    "dev": "esbuild src/main.jsx --bundle --jsx=automatic --outfile=app.js --watch"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
